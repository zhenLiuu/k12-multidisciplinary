# Changelog

所有影响公开数据内容、结构、标识、划分或打包方式的变化均记录于此。

## [Unreleased]

- v0.1.0 本地 GitHub/Hugging Face 发布候选已生成并验证，尚未上传远端。
- 训练集明确不进入首个公开版本，后续是否发布另行决定。
- 泄漏审计按维护者决定暂不执行。

## [0.1.0] - 2026-09-03

- 冻结首个公开候选的 `raw` 与 `test` split、15 个顶层字段及空值约定。
- 确定题目以 Zstandard 压缩的 Parquet 分片发布，目标 256 MiB、硬上限 512 MiB。
- 确定图片以不二次压缩的 POSIX tar 分片发布，目标 1 GiB、硬上限 1.25 GiB。
- 确定 GitHub 仅承载文档、代码、schema、小型审计摘要和清单；完整产物发布至 Hugging Face。
- 使用题干边界锚定规则移除高置信考试来源/学校标签；保留正文括号内容，并保守跳过会造成公开 ID 碰撞的记录。
- 完成图片打包、结构统一、公开 ID 生成、空白图处理和既有重复题裁决。
- 完成最终 schema 下的第二轮精确去重：16,568 个答案一致组删除 16,706 条冗余记录；115 个答案冲突组经 agy 裁决后再删除 128 条。
- 复查并解决此前保留的 4 个来源标签 ID 碰撞对：移除 4 个标签、删除 4 条副本；全量前缀规则复扫命中 0。
- 从 736,701 条最终数据独立重建索引，确认精确重复组、答案冲突组和跨 split 精确重复组均为 0；重新生成 ID 映射并通过 schema、ID、文本及全部图片内容哈希验证。
- 生成 2 个 Zstandard Parquet 题目分片和 9 个无外层压缩的图片 tar 分片，总计 9,472,318,316 字节。
- 通过 16 个发布文件 SHA-256、736,701 条记录逐条往返一致性以及 416,634 条图片索引与 tar 成员闭包验证。
